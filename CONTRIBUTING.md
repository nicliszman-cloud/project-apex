# Contributing to Project Apex

## Local setup

```bash
npm install
npx expo start
```

Use the demo mode until a Supabase project is configured.

## Before opening a PR

```bash
npm run typecheck
```

Keep secrets out of commits. Use `.env` locally and only commit `.env.example`.
